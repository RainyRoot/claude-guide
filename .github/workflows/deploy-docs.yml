# disabled for now, uncomment when you want to deploy to GitHub Pages
#
# steps to enable:
#   1. pip install mkdocs-material
#   2. set site_url in mkdocs.yml
#   3. uncomment everything below and push

# name: Deploy docs
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
# permissions:
#   contents: read
#   pages: write
#   id-token: write
# concurrency:
#   group: pages
#   cancel-in-progress: false
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#       - run: pip install mkdocs-material
#       - run: mkdocs build --strict
#       - uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./site
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v4
